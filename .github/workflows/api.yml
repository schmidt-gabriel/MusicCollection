name: Build and Release

on:
  push:
    tags: [ "**" ]

jobs:
  release-binaries:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        arch: [ amd64, arm64 ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: music_web_api/src/go.mod

      - name: Build binary
        working-directory: music_web_api/src
        env:
          CGO_ENABLED: 0
          GOOS: linux
          GOARCH: ${{ matrix.arch }}
        run: |
          mkdir -p "$GITHUB_WORKSPACE/dist"
          BIN_NAME="music_web_api-linux-${GOARCH}"
          go build -trimpath -ldflags="-s -w" -o "$GITHUB_WORKSPACE/dist/${BIN_NAME}" .

      - name: Upload binary to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/music_web_api-linux-${{ matrix.arch }}
